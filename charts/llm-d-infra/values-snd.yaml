gateway:
  provider: istio
  gatewayParameters:
    resources:
      limits:
        cpu: "2"
        memory: 1Gi
      requests:
        cpu: "250m"
        memory: 256Mi
    istio:
      accessLogging: false
